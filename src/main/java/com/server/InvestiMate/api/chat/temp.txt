1. 백엔드에서 유저로 부터 년도(Integer), 회사명(String), 보고서 종류(String)을 받는다. (3개의 args) -> 3개도 저장

2. python의 upload_files가 실행

3. 파이썬 코드 내부적으로 file_text.id, file_tables.id 반환

4. 어시스턴스 생성시에 이 이 파일아이디 들이 들어감. 이때 년도, 회사명, 보고서 종류, file.id

5. 어시스턴트의 id가 반환이되고  -> assistant_id DB에 저장 필요
ㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡ미리 작업해둠ㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡ
6. 독립적으로 쓰레드를 생성하여 id값을 반환 받음
    -> thread_id DB 저장필요 이때 thread의 만료 시간이 있기때문에 그것을 DB에 같이 저장해두고
        create_run()을 할때마다 thread_id를 통해 유효한지 아님 만료 시간이 지났는지를 검증한다.

7. 이제 언제든지 사용자의 질문을 input 받게 되면

8. 위에서 저장해둔 어시스턴트id, 쓰레드id, innstructions(사용자의 질문)를 인자로 넣어서 create_run()을 통해 event handler를 거쳐서 return이 오게됨.


